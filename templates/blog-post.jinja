<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <link href="/styles/styles.css" rel="stylesheet" />
    {% if mathjax %}
    <script defer src="/components/mathjax-config.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/mathjax@4/tex-chtml.js"></script>
    {% endif %}
</head>

<body>
    <h1 id="page-title">Blog Post</h1>
    {{ navbar }}
    <hr>
    {% for blog_post_navbar in blog_post_navbars %}
        {{ blog_post_navbar }}
        <hr>
    {% endfor %}
    <div class="blog-post">
        <h2 class="blog-post-title">{{ blog_post.title }}</h2>
        {% if blog_post["subtitle"] is string %}
        <p class="blog-post-subtitle" >{{ blog_post["subtitle"] }}</p>
        {% endif %}
        <p class="blog-post-date">{{ blog_post["date"] }}</p>
        {{ blog_post.content }}
    </div>
</body>

</html>